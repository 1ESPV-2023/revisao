//Criando a lista de tarefas
let listaTarefasArray = [];

//Recuperando o botão de envio
const botaoAddTarefa =  document.querySelector('btnAddTarefa')
botaoAddTarefa.addEventListener("click", (evt)=>{
    evt.preventDefault();
    
});